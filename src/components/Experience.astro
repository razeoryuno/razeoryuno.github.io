---
import { siteConfig } from "../config";
const hasExperience = siteConfig.experience && siteConfig.experience.length > 0;
---

{hasExperience && (
  <section id="experience" class="section-pad" aria-labelledby="experience-heading" style="background: var(--bg-panel)">
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-6 lg:gap-10 items-start">

      <!-- Section header -->
      <div class="lg:col-span-2">
        <p class="mono text-xs tracking-[0.2em] text-[var(--text-muted)] uppercase mb-2">SECTION_03</p>
        <h2 id="experience-heading" class="text-3xl lg:text-2xl xl:text-3xl font-bold text-[var(--text-primary)]">Experience</h2>
        <div class="w-12 h-0.5 mt-3 accent-rule"></div>
      </div>

      <!-- Timeline -->
      <div class="lg:col-span-10">
        <div class="pl-5 lg:mt-7">
          {siteConfig.experience.map((exp, index) => (
            <div class="pb-10 last:pb-0">
              <div class="relative ml-4">
                <!-- Dot -->
                <div
                  class="absolute -left-[41px] top-1/2 -translate-y-1/2 w-2.5 h-2.5 rounded-full border-2"
                  style={`border-color: ${siteConfig.accentColor}; background: var(--bg-base); z-index: 1`}
                ></div>
                <!-- Line: dot-center down to next entry (not last) -->
                {index < siteConfig.experience.length - 1 && (
                  <div class="absolute pointer-events-none" style="left: -36px; width: 1px; top: 50%; bottom: -40px; background: var(--border)"></div>
                )}
                <!-- Line: top of gap down to dot-center (not first) -->
                {index > 0 && (
                  <div class="absolute pointer-events-none" style="left: -36px; width: 1px; top: -40px; bottom: 50%; background: var(--border)"></div>
                )}
                <div class="card-panel p-4 sm:p-5">
                  <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-1 mb-3">
                    <div>
                      <h3 class="font-semibold text-[var(--text-primary)]">{exp.title}</h3>
                      <p class="text-sm" style={`color: ${siteConfig.accentColor}`}>{exp.company}</p>
                    </div>
                    <span class="mono text-xs text-[var(--text-secondary)] shrink-0">{exp.dateRange}</span>
                  </div>
                  <ul class="space-y-2">
                    {exp.bullets.map((bullet) => (
                      <li class="flex items-start gap-3 text-sm text-[var(--text-secondary)]">
                        <span class="mt-2 w-1 h-1 rounded-full shrink-0" style={`background: ${siteConfig.accentColor}`}></span>
                        {bullet}
                      </li>
                    ))}
                  </ul>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>

    </div>
  </section>
)}
